apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend
  namespace: day4
spec:
  type: ClusterIP
  selector:
    app: ecommerce
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
