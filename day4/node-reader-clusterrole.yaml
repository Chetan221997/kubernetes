apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: day4-node-reader
rules:
  - apiGroups:
      - ""
    resources:
      - nodes
    verbs:
      - get
      - list
      - watch
